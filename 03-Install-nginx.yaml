- name: Install Nginx
  hosts: web
  tasks:
  - name: Install Package
    ansible.builtin.package:
      name: nginx
      state: present

  - name: Start service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
