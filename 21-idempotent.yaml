- name: Idempotent
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: user add
    ansible.builtin.user:
      name: roboshop
      home: /app
      shell: /sbin/nologin
      state: present
      system: true